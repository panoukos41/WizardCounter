@inherits CoreComponent

<MenuButton class="@this.Class()">
    @(ChildContent?.Invoke(Menu?.IsOpen ?? false))
</MenuButton>

@code {
    [CascadingParameter]
    public Menu? Menu { get; set; }

    [Parameter]
    public RenderFragment<bool>? ChildContent { get; set; }
}
